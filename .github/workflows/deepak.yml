name: for testing purpose

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Access Secret
        run: echo "My secret key is ${{ secrets.MY_NAME }}"

      - name: Access Variable
        run: echo "My variable is ${{ vars.MY_AGE }}"

